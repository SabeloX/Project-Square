import styles from "./Navbar.module.css";

export const Navbar = () => {
    return (
        <nav className={`navbar ${styles.navContainer}`}>
            <div className="navbar-brand">
                <div className="navbar-item">sa</div>
            </div>
        </nav>
    )
}